{% extends "base.html.twig" %}

{% block body %}

    <div class="container">
        <br>
        <h3> Modifiez votre annonce : </h3>
        <br>
        {{ form_start(form) }}
        {{ form(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
    <br>
{% endblock %}